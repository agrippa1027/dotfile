set -g default-terminal "tmux-256color"
set -ga terminal-features ",xterm-ghostty:RGB,extkeys,clipboard,focus,hyperlinks,osc7,title"
set -ga terminal-features ",xterm-kitty:RGB,extkeys,clipboard,focus,hyperlinks,osc7,title"
set -sg escape-time 0
set -g history-limit 30000
set -g renumber-windows on
set -g base-index 1
setw -g pane-base-index 1
set -g mouse on
set -g focus-events on
set -g set-clipboard external
set -g mode-keys vi
set -g extended-keys always
set -g extended-keys-format csi-u

set -g prefix C-Space
bind C-Space send-prefix

# Theme
source-file ~/.config/tmux/themes/current.conf

bind r source-file ~/.config/tmux/tmux.conf \; display-message "tmux config reloaded"
bind h split-window -v -c '#{pane_current_path}'
bind v split-window -h -c '#{pane_current_path}'
bind x kill-pane

bind -n M-Enter split-window -v -c '#{pane_current_path}'
bind -n M-S-Enter split-window -h -c '#{pane_current_path}'
bind -n M-Escape kill-pane

bind -n C-M-Left select-pane -L
bind -n C-M-Down select-pane -D
bind -n C-M-Up select-pane -U
bind -n C-M-Right select-pane -R

bind -n C-h previous-window
bind -n C-j select-pane -D
bind -n C-k select-pane -U
bind -n C-l next-window

bind -n C-M-S-Left resize-pane -L 5
bind -n C-M-S-Down resize-pane -D 5
bind -n C-M-S-Up resize-pane -U 5
bind -n C-M-S-Right resize-pane -R 5

bind -n M-Left previous-window
bind -n M-Right next-window
bind -n M-Up switch-client -p
bind -n M-Down switch-client -n
bind -n C-S-o choose-tree -Zs

bind -n M-1 select-window -t :=1
bind -n M-2 select-window -t :=2
bind -n M-3 select-window -t :=3
bind -n M-4 select-window -t :=4
bind -n M-5 select-window -t :=5
bind -n M-6 select-window -t :=6
bind -n M-7 select-window -t :=7
bind -n M-8 select-window -t :=8
bind -n M-9 select-window -t :=9

bind [ copy-mode
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel
